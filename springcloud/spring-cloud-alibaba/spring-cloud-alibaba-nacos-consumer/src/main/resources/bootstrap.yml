server:
  port: 8082
spring:
  application:
    name: nacos-demo
  cloud:
    nacos:
      config:
        # nacos对应的配置文件名称为 ${prefix}-${spring.profiles.active}.${file-extension} ,即 nacos-demo.yaml
        file-extension: yaml
      server-addr: 192.168.0.222:8848
